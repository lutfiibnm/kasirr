import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lutfiibnm.galaksiaromatica',
  appName: 'Galaksi Aromatica POS',
  webDir: 'dist',
  server: { androidScheme: 'https' }
};

export default config;
